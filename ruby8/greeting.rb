# メソッドを定義するには、実行する処理内容をdef ～ end 内に記述する。
# メソッドを定義するときは、「引数」と「戻り値」も理解しておく必要がある。
# 「引数」は、メソッドを呼び出す際、メソッドに渡す値のことである。
# 「戻り値（返り値）」は、メソッドが呼ばれたときに返ってくる値のことである。
# →戻り値は、returnを使って明示的に指定することもできる。

 def greeting(name)
  return  "Hello, #{name}!"

   "Good morning, #{name}!"
 end

 puts greeting("John")
